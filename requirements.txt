# ============================================================
# FILE: requirements.txt
# PURPOSE: Lists all Python packages this project needs.
#          Run: pip install -r requirements.txt
#
# NOTE: Compatible with Python 3.10
# ============================================================

# ================= CORE =================
# Streamlit — turns Python into a web app (no HTML/CSS needed)
streamlit==1.23.1

# ================= AWS =================
# Boto3 — official AWS SDK for Python (S3, Bedrock, EC2, IAM, etc.)
boto3==1.34.0

# Botocore — low-level AWS library (boto3 depends on this)
botocore==1.34.0

# ================= UTILS =================
# python-dotenv — loads secrets from .env file into environment variables
python-dotenv==1.0.0
